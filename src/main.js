// Entry point (ES Module) - bootstraps existing app without changing behavior
// Loads the current implementation from agent.js (which attaches to window on DOMContentLoaded)
import '../agent.js';

export {};
